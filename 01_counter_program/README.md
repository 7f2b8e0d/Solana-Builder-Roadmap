# 🔢 链上计数器 Counter Program

## 📖 项目简介

这是Solana学习路线的第一个项目，一个简单但功能完整的链上计数器程序。通过这个项目，你将学习到：

- Solana账户模型和PDA（Program Derived Address）
- Anchor框架的基本使用
- 智能合约的状态管理
- 链上程序的权限控制
- TypeScript测试编写

## 🏗️ 项目结构

```
01_counter_program/
├── programs/
│   └── counter_program/
│       └── src/
│           └── lib.rs          # 主程序文件
├── tests/
│   └── counter_program.ts      # 测试文件
├── Anchor.toml                 # Anchor配置
└── README.md                   # 项目说明
```

## 🚀 功能特性

### 核心功能
- **初始化计数器** - 创建一个新的计数器账户
- **增加计数** - 将计数器值加1
- **减少计数** - 将计数器值减1
- **重置计数器** - 将计数器重置为0（仅权限用户）

### 技术特点
- 使用PDA确保账户地址的确定性
- 实现权限控制，只有创建者可以重置
- 完整的错误处理和日志记录
- 全面的测试覆盖

## 📋 核心概念学习

### 1. PDA (Program Derived Address)
```rust
#[account(
    init,
    payer = user,
    space = 8 + 8 + 32, // discriminator + count + authority
    seeds = [b"counter"],
    bump
)]
pub counter: Account<'info, Counter>,
```

### 2. 账户结构定义
```rust
#[account]
pub struct Counter {
    pub count: i64,        // 计数值
    pub authority: Pubkey, // 权限用户
}
```

### 3. 指令处理
```rust
pub fn increment(ctx: Context<Increment>) -> Result<()> {
    let counter = &mut ctx.accounts.counter;
    counter.count += 1;
    msg!("计数器增加！当前值: {}", counter.count);
    Ok(())
}
```

## 🛠️ 快速开始

### 环境要求
- Rust 1.70+
- Solana CLI 1.18+
- Anchor Framework 0.32+
- Node.js 16+

### 安装依赖
```bash
# 安装Node.js依赖
yarn install

# 构建程序
anchor build
```

### 运行测试
```bash
# 启动本地验证器
solana-test-validator

# 运行测试（新终端）
anchor test
```

### 部署到Devnet
```bash
# 配置网络
solana config set --url devnet

# 部署程序
anchor deploy
```

## 🧪 测试用例

项目包含完整的测试套件：

1. **初始化测试** - 验证计数器正确初始化
2. **增加测试** - 验证计数器能正确增加
3. **减少测试** - 验证计数器能正确减少
4. **重置测试** - 验证权限控制和重置功能

## 📚 学习要点

通过这个项目，你将掌握：

### Solana基础概念
- ✅ Account模型和所有权
- ✅ Transaction和Instruction
- ✅ 程序ID和PDA
- ✅ Rent和账户生命周期

### Anchor框架
- ✅ 项目结构和配置
- ✅ `#[program]`和`#[derive(Accounts)]`
- ✅ 账户约束和验证
- ✅ 错误处理

### 开发实践
- ✅ 测试驱动开发
- ✅ 日志和调试
- ✅ 部署和验证

## 🔗 相关资源

- [Solana官方文档](https://docs.solana.com/)
- [Anchor框架文档](https://www.anchor-lang.com/)
- [Solana Cookbook](https://solanacookbook.com/)

## 📝 下一步

完成这个项目后，你可以继续学习：
- 阶段2：PDA与确定性账户
- 阶段3：Anchor入门与Program结构
- 更复杂的数据结构和多账户协作

---

🎉 **恭喜！你已经完成了第一个Solana程序！**